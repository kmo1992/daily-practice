# Claude Code Instructions

## Workflow
- Always push the working branch to the remote before the session ends.
- Before ending a session, detach HEAD in the worktree (`git checkout --detach`) so the branch is not locked and can be checked out in GitHub Desktop.
- The user wants to test changes locally by switching to the branch in GitHub Desktop.
